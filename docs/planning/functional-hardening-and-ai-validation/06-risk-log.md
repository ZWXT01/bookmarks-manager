# bookmarks-manager 风险点台账

更新时间：2026-04-02

关联文档：

- [执行路线图](./02-roadmap.md)
- [Issue 拆分](./03-issue-breakdown.md)
- [Agent 执行手册](./04-agent-runbook.md)
- [Agent 进度台账](./05-agent-status.md)

## 1. 说明

- 本文件是风险点的唯一台账，也是“风险排查执行模式”的唯一状态源。
- 风险点不能只停留在一次性汇报里，后续执行必须持续回写本文件。
- 每条风险记录至少应包含：`issue_id`、日期、影响范围、触发条件或现象、当前缓解方式、后续排查建议。
- 除风险本身的生命周期状态外，还应记录修复执行状态、承接 issue 与 Git commit。
- 若风险已消除，应更新 `risk_status`，而不是直接删除历史记录。
- secrets 泄露、端口暴露、误删数据、权限扩大、异常进程和高风险误操作都应进入本台账。

`risk_status` 枚举：

- `open`
- `mitigated`
- `resolved`

`fix_status` 枚举：

- `todo`
- `in_progress`
- `blocked`
- `done`

## 2. 风险排查执行规则

- 优先读取本文件。
- 自动选择第一个 `fix_status = todo` 的风险项。
- 开始前写入 `started_at`，并将 `fix_status` 改为 `in_progress`。
- 完成后写入 `completed_at`，并更新 `risk_status` 与 `fix_status`。
- 若阻塞则写入 `blocked_reason`，并将 `fix_status` 改为 `blocked`。
- 完成提交后回写 `git_commit`。

## 3. 风险列表

| risk_id | date | issue_id | area | risk_status | fix_status | fix_issue_id | git_commit | started_at | completed_at | blocked_reason | risk | current_mitigation | follow_up |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `RISK-001` | 2026-03-28 | `R15-AI-01` | AI provider contract | resolved | done | `R15-AI-01,R5-AI-01,R5-AI-02,R5-AI-03,R5-AI-04,R5-AI-05,R5-AI-06,R5-AI-07,R5-AI-08,R5-AI-09` | `061bb0f,c3ad87c,49515ec,b457d50,1b98512,e7face9,8dfffca,c44f362,d9c24ef,5a8cdcd` | 2026-03-28 20:50:01 +0800 | 2026-03-30 15:34:32 +0800 |  | 真实 OpenAI 兼容模型可能不支持“联网浏览”或稳定 JSON 输出，导致 `classify` / `organize` 质量和回归结果不可重复。 | `R15-AI-01` 已引入可注入 AI client factory、队列式 fixture harness；`R15-H1-04` 又用固定 3 条书签样本完成真实 provider 验收，确认 `/api/ai/test`、`classify-batch`、`organize`、`apply/rollback` 均可用。`R5-AI-01` 到 `R5-AI-08` 逐步补齐了 taxonomy guardrail、单条 classify 语义择优、固定样本集、timeout fallback、`/api/ai/test` 瞬时重试、`/models` 诊断与设置页诊断可见性。`R5-AI-09` 又把真实 provider 验证默认源收口为本地 `validation_grok_*`，修正了可用的 Grok endpoint `https://grok2api.1018666.xyz/v1`，并为 AI client / classify / organize 增加了对 `text/event-stream` `data: ... delta.content` completion 的统一解析与 `<think>` 清理。随后用默认 Grok 源重跑 direct diagnose、focused H1 和 full H1，确认 `/models = 200`、`modelFound = true`、`modelCount = 15`、`/chat/completions = 200`，并恢复到 `singleAccepted = 1/1`、`batchAccepted = 3/3`、`organizeAccepted = 3/3`、`apply_count = 3`、`rollback restored_bookmarks = 3`。 | 当前 release gate 下，默认 Grok provider 验证已经闭环。后续若手工切换 provider / endpoint 或新增其它 OpenAI 兼容源，应先复跑 `scripts/ai-provider-diagnose.ts`、`scripts/ai-h1-classify-semantic-validate.ts`、`scripts/ai-h1-validate.ts`；若不想使用 Grok 默认源，需显式传 `--provider current`。 |
| `RISK-002` | 2026-03-28 | `R1-QA-01` | UI verification drift | resolved | done | `R1-QA-01` | `e719860` | 2026-03-28 15:28:57 +0800 | 2026-03-28 15:37:03 +0800 |  | 仓库内 `e2e/` 仍然存在，但当前执行策略已切换为内置 Playwright MCP；若不明确主次，后续会出现两套 UI 验证标准并行漂移。 | `R1-QA-01` 已完成：最小 smoke checklist 已固化到 `08-playwright-mcp-smoke-baseline.md`，仓库内 Playwright 也已明确为非 release gate 历史资产。 | 后续扩展 UI 验证时，只在内置 Playwright MCP 基线上追加旅程，不再把仓库内 Playwright 恢复为当前主 gate。 |
| `RISK-003` | 2026-03-28 | `R1-BE-03` | Backup / restore contract | resolved | done | `R1-BE-03` | `741d81e` | 2026-03-28 16:21:20 +0800 | 2026-03-28 16:49:56 +0800 |  | 当前备份生成的是整库文件，但还原实现只复制 `categories` 和 `bookmarks`，其余表和资产不在显式恢复范围内。 | `R1-BE-03` 已将 restore 固化为“临时副本校验 + `pre_restore_*.db` 回滚点 + 事务性替换 `categories` / `bookmarks`”的显式部分恢复合同，并用临时目录集成测试覆盖命名备份、restore 与 rollback。 | 若未来要把 restore 扩展到完整恢复或更多业务表，必须另立新 issue，不得在当前 partial-restore 合同内隐式扩大范围。 |
| `RISK-004` | 2026-03-28 | `R1-BE-03` | Snapshot storage | resolved | done | `R1-BE-03` | `741d81e` | 2026-03-28 16:21:20 +0800 | 2026-03-28 16:49:56 +0800 |  | `snapshots` 表在路由层懒初始化，HTML 快照文件在文件系统中，和数据库 schema / 备份逻辑分离。 | `R1-BE-03` 已把 `snapshots` schema 收口到 `src/db.ts`，并在合同文档中明确：快照元数据与 HTML 文件资产当前保留但不纳入 restore 覆盖范围，restore 后原书签绑定也不再被自动保证。 | 若未来需要快照文件随备份一起灾备，或需要 restore 后自动重建快照与书签绑定关系，应另立后续 issue。 |
| `RISK-005` | 2026-03-28 | `R1-DOC-04` | Docs and UI drift | resolved | done | `R1-DOC-04` | `6187281` | 2026-03-28 16:53:17 +0800 | 2026-03-29 16:28:46 +0800 |  | README 曾声明 `ai_simplify`、旧模块文件名和旧扩展目录；任务页也保留过 simplify 遗留变量，容易误导测试与验收。 | `6187281` 已清理 README、旧扩展路径、过时 API 描述和任务页遗留建议面板；2026-03-29 又用本地临时服务 + 内置 Playwright MCP 补跑 `/login` 与 `/jobs`，标题与页面渲染正常，`warning/error` 计数为 `0`。 | 后续若要恢复 simplify 或扩展新的历史任务类型，必须另立 issue，不能重新把 `ai_simplify` 当成当前活跃功能。 |
| `RISK-006` | 2026-03-28 | `R15-AI-03` | AI settings drift | resolved | done | `R15-AI-03` | `a5ff4c9` | 2026-03-29 00:19:38 +0800 | 2026-03-29 00:45:20 +0800 |  | 设置页存有 `ai_batch_size`，但 AI 路由默认使用请求体 `batch_size`，导致配置含义不一致。 | `R15-AI-03` 已将 `ai_batch_size` 标准化为运行时权威默认值，并用 classify-batch、organize、retry、settings reset 的自动化用例证明配置实际生效。 | 在 `R15-H1-04` 只继续验证真实 provider 质量，不再把 batch-size 配置合同视为主要风险。 |
| `RISK-007` | 2026-03-28 | `R1-API-02` | Coverage blind spots | mitigated | done | `R1-API-02` | `8224ae7` | 2026-03-28 15:42:55 +0800 | 2026-03-28 15:55:03 +0800 |  | 模板、快照、备份、设置和 AI 路由仍存在明显自动化盲区，容易在重构中无声回归。 | `R1-API-02` 已补齐设置 / 模板 / 快照 / 备份的独立自动化覆盖；当前剩余盲区主要收敛到 AI 路由合同，由 `R15-AI-02`、`R15-AI-03` 继续处理。 | 后续只保留 AI 相关盲区跟踪，不再把非 AI 运维面视为主要自动化空白。 |
| `RISK-008` | 2026-03-28 | `R2-EXT-02` | Browser extension round-trip | resolved | done | `R2-EXT-02,R5-EXT-02,R5-EXT-03` | `5b70fea,9b4db2d,95d48a3` | 2026-03-29 15:23:49 +0800 | 2026-03-29 22:50:05 +0800 |  | `extension-new/` 负责书签 / 快照保存；如果只验证 popup-harness 或“直接打开 popup.html”，就无法证明真实 browser action popup target 是否真的绑定到当前活动页。 | `R2-EXT-02` 已修正扩展提交合同并补上 popup-harness；`R5-EXT-02` 又把验收升级到真实 Chromium unpacked extension runtime，覆盖真实 `chrome.storage`、tab 查询、content script 注入和保存链路；`R5-EXT-03` 最后用 `scripts/extension-action-popup-validate.ts` + `20-extension-action-popup-validation.md` 验证了真实 action popup target 可被打开、附着，并能看到当前活动页的标题 / URL 与 active tab。 | 当前不再存在扩展宿主层的主要合同盲区。后续若改 action popup、权限或 tab 绑定逻辑，只需复跑 `scripts/extension-runtime-validate.ts` 和 `scripts/extension-action-popup-validate.ts`，无需再额外为“工具栏物理点击手势”单独立项。 |
| `RISK-009` | 2026-03-28 | `R2-E2E-01` | Frontend asset integrity | resolved | done | `R2-E2E-01,R5-UI-04` | `81db8a6,94dc200` | 2026-03-29 14:33:28 +0800 | 2026-03-29 23:54:00 +0800 |  | 测试环境首页对 `OverlayScrollbars` CSS / JS 配置的 `integrity` 值与实际资源不匹配，浏览器直接阻断加载；同时缺少 `favicon.ico` 且多个页面仍依赖运行时 `tailwind.js`，控制台持续报错 / 警告并留下生产资产漂移隐患。 | `R2-E2E-01` 已先移除首页错误的第三方脚本 `integrity` 并补上 `/favicon.ico -> /public/favicon.svg`；`R5-UI-04` 又新增 `scripts/generate-static-tailwind.ts` 与受版本控制的 `public/tailwind.generated.css`，并把首页 / 登录 / 设置 / 任务 / 快照页统一切到静态 `<link>`。`tests/integration/page-assets.test.ts` 现在会阻止运行时 Tailwind `<script>` 和 warning suppress shim 回退，`scripts/category-interaction-validate.ts` 也已证明静态样式替换后首页分类导航与跨页面交互主链路仍然正常。 | 后续若模板或前端脚本新增了新的 utility class，先复跑 `npx tsx scripts/generate-static-tailwind.ts`、`npm test` 和 `npx tsx scripts/category-interaction-validate.ts`，再提交静态 CSS 产物。`public/lib/tailwind.js` 当前只保留为生成脚本输入，不再作为页面运行时依赖。 |
| `RISK-010` | 2026-03-28 | `R1-QA-05` | Test scaffolding drift | resolved | done | `R1-QA-05` | `50a05d9` | 2026-03-28 16:04:06 +0800 | 2026-03-28 16:17:28 +0800 |  | 当前工作区同时存在已跟踪测试、未纳管测试骨架和重复 helper 入口；如果继续在这些脚手架上叠加用例，后续提交容易出现“本地能跑但 commit 不自洽”的情况。 | `R1-QA-05` 已正式纳管 `tests/helpers/*` 与 `tests/integration/*`，删除重复的 `tests/bookmarks-routes.test.ts`，并把运维面覆盖迁入 `tests/integration/ops-routes.test.ts` 复用统一 `createTestApp()` 主路径。 | 后续新增 app 级测试统一走 `tests/helpers/app.ts`、`tests/helpers/auth.ts`、`tests/helpers/factories.ts`；UI 验证仍维持内置 Playwright MCP 主路径，不再回退到仓库内 Playwright。 |
| `RISK-011` | 2026-03-29 | `R3-UI-01` | Frontend UI stability | resolved | done | `R3-UI-01` | `4abf7ee` | 2026-03-29 17:09:40 +0800 | 2026-03-29 17:22:52 +0800 |  | 首页分类导航曾出现“初始水平、刷新后转为垂直”的布局漂移，且滚动 / 拖拽失效时会导致部分分类不可达。 | `R3-UI-01` 已移除分类导航对 `OverlayScrollbars` 的宿主级包裹，改回原生横向滚动，并补上按钮、滚轮、桌面端拖拽和触屏横滑的等价访问方式；`scripts/category-nav-validate.ts` 与 `14-category-nav-validation.md` 已证明初次加载和刷新后都保持单行横排。 | 后续若再改首页分类导航结构，必须先复跑 `npx tsx scripts/category-nav-validate.ts`，避免重新引入 hydration / layout 漂移。 |
| `RISK-012` | 2026-03-29 | `R3-AI-02` | AI organize data safety | resolved | done | `R3-AI-02` | `550481d` | 2026-03-29 17:35:28 +0800 | 2026-03-29 19:21:29 +0800 |  | AI organize 会改动书签与分类；若在 plan 生成后用户又移动、编辑、删除书签或分类，或多个 organize 任务交错 apply，当前仍可能出现误应用、脏写或数据丢失风险。 | `R3-AI-02` 已为 preview 固化 `source_snapshot`，把书签删除 / 分类漂移 / 模板变更 / 重叠 plan stale 都改为显式 `409` guardrail，并停止在 apply 阶段静默重建缺失分类；`15-ai-organize-safety-validation.md` 与 `tests/integration/ai-organize-routes.test.ts` 已覆盖高风险组合。 | 升级前遗留且没有 `source_snapshot` 的旧 preview plan 现在会被拒绝应用；如现场仍有这类计划，需要重新执行 organize 生成新 preview。 |
| `RISK-013` | 2026-03-29 | `R3-QA-03` | Cross-view interaction consistency | resolved | done | `R3-QA-03` | `aa7a414` | 2026-03-29 19:03:40 +0800 | 2026-03-29 19:21:29 +0800 |  | 分类管理页排序、首页分类导航顺序、分类下拉 / 筛选等跨页面状态可能不一致；拖拽、移动或删除后如果不同步，用户会看到错误导航或产生误操作。 | `R3-QA-03` 已新增 `scripts/category-interaction-validate.ts`，真实驱动分类管理弹窗中的拖拽排序，并验证首页导航与“添加书签”分类下拉同步更新且刷新后保持一致；验收记录已写入 `16-category-interaction-validation.md`。 | 后续若再改分类导航、管理弹窗或依赖 `categories` 顺序的表单，必须先复跑 `scripts/category-interaction-validate.ts`；若要继续扩展 delete / move / template-switch 联动，可另在同一 harness 上追加场景。 |
| `RISK-014` | 2026-03-29 | `R4-CLEAN-01` | Legacy AI runtime drift | resolved | done | `R4-CLEAN-01` | `307f95b` | 2026-03-29 19:40:27 +0800 | 2026-03-29 21:03:07 +0800 |  | `ai_simplify` 已被产品口径下线，但运行时代码仍保留可误导维护者的专属任务类型与任务页分支，后续很容易被误当成当前功能继续串接。 | `R4-CLEAN-01` 已把 `createJob()` 收口成只接受当前活跃类型，并把任务详情页的 `ai_simplify` 专属 UI 分支统一收口为“历史 AI 任务（已下线）”；功能矩阵也同步改成“仅保留历史兼容读取和旧表迁移清理”。 | 若未来真的要恢复 simplify，不得直接复用历史类型残留，必须另立新 issue 明确路由、provider、验收和回滚合同。 |
| `RISK-015` | 2026-03-29 | `R4-QA-02` | Cross-view state invalidation | resolved | done | `R4-QA-02` | `e09a1a0` | 2026-03-29 19:40:27 +0800 | 2026-03-29 21:03:07 +0800 |  | 删除分类、模板切换等会重建分类树的操作，可能让前端仍保留失效的 `currentCategory` / 展开状态 / 已选分类，出现“导航已变、列表仍是旧筛选结果”的误导性 UI。 | `R4-QA-02` 已在 `loadCategories()` 中新增统一的分类 UI 状态标准化，自动收口失效筛选、失效展开父分类和失效已选分类；同时把 `scripts/category-interaction-validate.ts` 扩展到删除分类、子分类移动、单条 / 批量书签移动、模板切换和刷新保持，并新增 `17-cross-view-interaction-validation.md` 留痕。 | 后续若再改分类刷新、模板切换或书签移动链路，必须先复跑 `npx tsx scripts/category-interaction-validate.ts`，避免重新引入 stale state 漂移。 |
| `RISK-016` | 2026-03-29 | `R5-AI-01` | Single classify taxonomy drift | resolved | done | `R5-AI-01` | `c3ad87c` | 2026-03-29 21:32:12 +0800 | 2026-03-29 21:42:02 +0800 |  | 单条 `/api/ai/classify` 原本只做“最多两级”截断，不校验是否落在当前模板 / 分类树内，因此真实 provider 会把 `React 官方文档` 返回成模板外层级 `学习资源/React`。 | `R5-AI-01` 已新增候选分类枚举、路径标准化与 taxonomy guardrail：优先读取活动模板，否则回退 live categories；`学习资源/React` 这类结果会被归一化到 `学习资源/文档`，完全不可映射的结果则直接返回错误，避免透传错误分类。 | 这条风险已经在路由合同层关闭；后续若要继续提升的是“模板内分类是否足够准确”，不再是“会不会输出模板外路径”。 |
| `RISK-017` | 2026-03-30 | `R6-AI-01` | Organize apply contract | resolved | done | `R6-AI-01` | `244571c` | 2026-03-30 16:33:43 +0800 | 2026-03-30 17:02:56 +0800 |  | 多个 `preview` organize plan 同时存在时，当前是否“只允许最新 plan 应用”并不直观；实际行为还取决于模板和书签集合是否重叠，操作者很容易误判可应用范围。 | `R6-AI-01` 已把 organize apply 合同收口为“同模板不重叠直接 apply、跨模板继续按模板快照隔离 apply、同模板重叠进入显式冲突解决”。`src/ai-organize-plan.ts` 现在把分类漂移、书签更新与重叠 plan 拆成可见冲突项；`views/job.ejs` 会展示冲突原因；`tests/integration/ai-organize-routes.test.ts` 与 `30-organize-apply-contract-validation.md` 已覆盖重叠 resolve、非重叠直应用和跨模板隔离写入。 | 后续若继续改 organize apply / resolve / rollback 合同，必须先复跑 `tests/integration/ai-organize-routes.test.ts` 和全量 `npm test`，避免重新回到“只能靠 stale 拒绝或读源码理解”的隐式行为。 |
| `RISK-018` | 2026-03-30 | `R6-UI-02` | Template editor modal accessibility | resolved | done | `R6-UI-02` | `84740bd` | 2026-03-30 19:18:07 +0800 | 2026-03-30 19:25:06 +0800 |  | 自定义模板分类项不断增多时，模板编辑弹窗会持续拉长，长树编辑下保存 / 取消等关键操作可能被挤出可视区，尤其在小视口或软键盘场景更明显。 | `R6-UI-02` 已把模板选择 / 编辑弹窗改成显式视口高度边界和固定头尾布局，不再依赖脆弱的 `max-h-[80vh]` / `max-h-[85vh]` 任意值类名；同时新增稳定选择器、`tests/integration/page-assets.test.ts` 壳体回归，以及 `scripts/template-editor-validate.ts` 浏览器级长树验证，证明在 `960x560` 小视口下滚到最底部时保存 / 取消仍在视口内可用。 | 后续若继续改模板弹窗结构、长树编辑行为或模板选择弹窗壳体，必须先复跑 `npx vitest run tests/integration/page-assets.test.ts --reporter=verbose` 与 `npx tsx scripts/template-editor-validate.ts`，避免再次回到“DOM 里有按钮但视口里不可达”的状态。 |
| `RISK-019` | 2026-03-30 | `R6-AI-03` | AI template source drift | resolved | done | `R6-AI-03` | `3dc0cf1` | 2026-03-30 19:38:52 +0800 | 2026-03-30 19:49:39 +0800 |  | 模板编辑后，AI 分类入口如果仍读取旧模板树、旧候选分类或陈旧 snapshot，就会出现“界面模板已变、AI 仍按旧分类分类”的隐蔽漂移。 | `R6-AI-03` 已把 organize plan 在创建时冻结为明确的 `target_tree + source_snapshot.template`，`assignBookmarks()` 统一基于 plan 快照而不是 live categories 生成候选分类；默认单条 `classify`、`classify-batch` 和 `organize` 在模板编辑后都会跟随最新活动模板，显式 `template_id` 的 organize 仍保持跨模板隔离。新增的 `tests/integration/ai-routes.test.ts`、`tests/integration/ai-organize-routes.test.ts`、`tests/integration/ai-harness.test.ts` 还证明了 assigning 中途改模板时旧 preview 会被判 stale，而不会把旧分类伪装成新模板结果。 | 后续若继续改模板 CRUD、AI 默认入口或 organize preview 生命周期，必须先复跑 `npx vitest run tests/integration/ai-routes.test.ts --reporter=verbose`、`npx vitest run tests/integration/ai-organize-routes.test.ts --reporter=verbose`、`npx vitest run tests/integration/ai-harness.test.ts --reporter=verbose` 和全量 `npm test`，避免重新引入“默认入口读活表、preview 读旧树”的漂移。 |
| `RISK-020` | 2026-03-30 | `R6-EXT-04` | Built-in extension UI debt | resolved | done | `R6-EXT-04` | `7ebb669` | 2026-03-30 20:01:48 +0800 | 2026-03-30 20:17:24 +0800 |  | 项目内置扩展目前已覆盖功能合同，但 popup 仍偏工程化：主操作、设置区、分类选择和状态反馈的层次感不足，影响可读性和使用体验。 | `R6-EXT-04` 已重构 `extension-new/popup.html`、`popup.css`、`popup.js` 的信息架构，把 popup 收口为清晰的三段式界面：当前页面、主操作、连接设置；同时新增设置摘要、分类摘要、明确的 loading / success / error 状态卡、主次分明的按钮层级和按钮 busy 恢复。`tests/extension-popup-shell.test.ts` 负责守住关键结构锚点，`scripts/extension-runtime-validate.ts` 则已在真实扩展宿主里证明新的 popup UI shell、主操作层级、设置区展开、成功 / 失败反馈和原有保存 / 快照主链路全部通过。 | 后续若继续改 popup 视觉结构、按钮文案、连接状态或设置区交互，必须先复跑 `npx vitest run tests/extension-popup-shell.test.ts --reporter=verbose`、`npx tsx scripts/extension-runtime-validate.ts`、全量 `npm test` 和 `npm run build`，避免把 popup 又退回“功能在，但信息层次和反馈混杂”的状态。 |
| `RISK-021` | 2026-03-30 | `R6-EXT-05` | Built-in extension SingleFile robustness | resolved | done | `R6-EXT-05` | `f1fdfe6` | 2026-03-30 22:38:15 +0800 | 2026-03-30 23:03:17 +0800 |  | 内置扩展基于 SingleFile 做快照，注入失败、页面不可连接、重复点击、长页面超时或大内容上传时，仍可能出现状态恢复不稳、错误提示粗糙或资源开销偏大的问题。 | `R6-EXT-05` 已把 `extension-new/popup.js` / `content.js` 的快照链路收口为“先检查目标页与 bridge，再进入保存”：popup 现在会先 ping 现有 capture bridge，只在缺失时再注入脚本；`saveAll()` 会在保存书签前先拒绝不支持快照的页面，避免出现“书签保存了、快照必失败”的半成功状态；content script 也新增了 in-flight guard 与 capture timeout。`tests/extension-popup-shell.test.ts` 与 `scripts/extension-runtime-validate.ts` 现在已证明不支持页面、目标页失效、超时恢复和重复 `save-all` 点击都能给出可见错误并恢复按钮状态，且不会制造重复书签 / 快照。 | 后续若继续改 SingleFile vendor、`popup.js`、`content.js` 或扩展权限 / 注入策略，必须先复跑 `npx vitest run tests/extension-popup-shell.test.ts --reporter=verbose`、`npx tsx scripts/extension-runtime-validate.ts`、全量 `npm test` 和 `npm run build`，避免重新引入“误注入、误报成功、按钮卡死或重复提交”的回归。 |
| `RISK-022` | 2026-03-30 | `R6-TPL-06` | Preset template coverage debt | resolved | done | `R6-TPL-06` | `a3c6587` | 2026-03-30 23:18:30 +0800 | 2026-03-30 23:49:11 +0800 |  | 当前内置预置模板数量偏少，用户一旦组织场景偏离默认模板，就容易被迫从零编辑长树模板，放大模板编辑成本和 AI 分类 taxonomy 不贴合的问题。 | `R6-TPL-06` 已把预置模板库从 4 套扩到 8 套：新增 `产品运营版`、`内容创作版`、`研究学习版`、`收藏归档版`，并在模板库卡片上补了“创建副本 / 创建并应用”入口。`scripts/preset-template-validate.ts` 已在真实浏览器里证明“模板库创建副本 -> 应用自定义模板 -> 首页导航 / 分类管理同步切换 -> AI 单条 classify 默认候选分类跟随活动模板刷新”整条链路成立；`tests/integration/ops-routes.test.ts` 与 `tests/integration/page-assets.test.ts` 也守住了预置模板种子数量和稳定选择器。 | 后续若继续增删预置模板、改模板库 UI 或调整默认 AI 分类入口，必须先复跑 `npx vitest run tests/integration/ops-routes.test.ts tests/integration/page-assets.test.ts --reporter=verbose`、`npx tsx scripts/preset-template-validate.ts`、全量 `npm test` 和 `npm run build`，避免重新回到“模板库数量变了，但切换链路和 AI 默认入口没跟上”的状态。 |
| `RISK-023` | 2026-03-31 | `R7-AI-01` | AI organize assigning timing | resolved | done | `R7-AI-01` | `bb66b12` | 2026-03-31 00:03:28 +0800 | 2026-03-31 00:20:22 +0800 |  | `createPlan()` 已经限制同一时间只能有 1 个 `assigning` plan，但 `retry` 之前仍可能绕过这条单活锁；同时 canceled plan 的 in-flight provider 响应也可能在取消后继续写 `assignments`、`batches_done`、`source_snapshot` 或 `preview`，把旧结果污染回数据库。 | `R7-AI-01` 已把 `assigning` 槽位检查提成统一 helper，并同时接到 `createPlan()` 和 `transitionStatus(..., 'assigning')`；`cancel` 与 in-flight provider 返回之间也新增了多处停止写回检查。`tests/ai-organize-plan.test.ts` 与 `tests/integration/ai-organize-routes.test.ts` 现已覆盖 start / retry 的 `409 + activePlanId`、canceled in-flight plan 不再写 stale preview、missing-plan cancel 返回 `404`。 | 若后续继续改 `transitionStatus()`、`assignBookmarks()`、`cancel/retry` 路由或 `JobQueue` 执行语义，必须先复跑 `npx vitest run tests/ai-organize-plan.test.ts tests/integration/ai-organize-routes.test.ts --reporter=verbose`、全量 `npm test` 与 `npm run build`。 |
| `RISK-024` | 2026-03-31 | `R7-AI-02` | AI organize scope drift | resolved | done | `R7-AI-02` | `7919bad` | 2026-03-31 00:30:12 +0800 | 2026-03-31 00:43:40 +0800 |  | organize plan 之前只冻结模板树和 apply 安全快照，没有冻结“这次到底整理哪些书签”；`all / uncategorized / category:N` 在 failed plan retry 或延迟执行时会重新读取 live scope，把后来新增的书签吸进原计划，或让原计划的处理范围静默漂移。 | `R7-AI-02` 已把 `scope_bookmark_ids + scope_frozen` 写入 `source_snapshot`，并让 `createPlan()`、`assignBookmarks()`、`transitionStatus(..., 'assigning')` 共用这份作用域快照。`tests/ai-organize-plan.test.ts` 现已证明 plan 创建时就冻结初始作用域；`tests/integration/ai-organize-routes.test.ts` 也证明 failed plan retry 后不会把后来新增的书签吸进原计划。 | 后续若继续改 organize scope 语义、retry 行为或 `source_snapshot` 结构，必须先复跑 `npx vitest run tests/ai-organize-plan.test.ts tests/integration/ai-organize-routes.test.ts --reporter=verbose`、全量 `npm test` 与 `npm run build`。 |
| `RISK-025` | 2026-03-31 | `R7-AI-03` | AI organize retry stale state | resolved | done | `R7-AI-03` | `60cd9af` | 2026-03-31 19:14:10 +0800 | 2026-03-31 19:22:24 +0800 |  | failed plan `retry` 之前缺少 AI 配置预检，可能把 plan 推进到 `assigning` 但没有任何 worker 入队；同时旧失败 run 的 `assignments`、`failed_batch_ids`、批次计数和 diff 也可能在 retry 的新运行阶段短暂继续暴露。 | `R7-AI-03` 已把 AI 配置校验前置到 retry 入口，并在 `transitionStatus(..., 'assigning')` 时清理旧失败产物，只保留冻结后的 scope / template 快照。`tests/ai-organize-plan.test.ts` 与 `tests/integration/ai-organize-routes.test.ts` 现在已覆盖“缺配置 retry 不推进状态”和“retry assigning 阶段不暴露旧 preview 数据”的合同。 | 后续若继续改 retry 路由、`transitionStatus()` 或 failed plan 详情页展示逻辑，必须先复跑 `npx vitest run tests/ai-organize-plan.test.ts tests/integration/ai-organize-routes.test.ts --reporter=verbose`、全量 `npm test` 与 `npm run build`。 |
| `RISK-026` | 2026-03-31 | `R7-AI-04` | AI organize stale frozen scope gap | resolved | done | `R7-AI-04` | `001039c` | 2026-03-31 19:37:47 +0800 | 2026-03-31 19:52:00 +0800 |  | organize plan 虽然已经冻结了 `scope_bookmark_ids`，但 worker 真正执行时如果其中有书签已被删除，当前实现会静默缩小处理集合继续 preview；同时 assigning 阶段若抛出致命异常，job 可能已经 `failed`，但 plan 仍卡在 `assigning`。 | `R7-AI-04` 已在 `assignBookmarks()` 中把“冻结 scope 缺对象”收口为显式 stale `error`，并让 start / retry 共用这条合同；worker 侧致命异常也统一落到 `plan = error`、`job = failed + message`，不再留下假活跃的 assigning plan。`tests/integration/ai-organize-routes.test.ts` 已覆盖 ids scope 缺对象 start 和 retry 前删除冻结 scope 书签两类场景。 | 后续若继续改 `assignBookmarks()`、scope 解析、retry 路由或 organize job 异常处理，必须先复跑 `npx vitest run tests/integration/ai-organize-routes.test.ts --reporter=verbose`、全量 `npm test` 与 `npm run build`，避免重新回到“scope 已冻结但执行时又静默缩水”的状态。 |
| `RISK-027` | 2026-04-01 | `R7-AI-05` | AI organize error phase visibility | resolved | done | `R7-AI-05` | `6155509` | 2026-04-01 20:41:29 +0800 | 2026-04-01 20:44:23 +0800 |  | `R7-AI-04` 之后 organize plan 已可能进入 `error`，但 `/retry` 仍先做 AI 配置校验，导致 missing plan / invalid status 会被配置错误盖住；首页 modal 也只认识 `failed`，可能在 plan 进入 `error` 后继续停在 `assigning`，而用户看不到具体中断原因。 | `R7-AI-05` 已把 `error -> assigning` 定义成显式 retry 合同，并把 `/api/ai/organize/:planId/retry` 的判定顺序收口为“先判 plan 是否存在 / 是否可重试，再判 AI 配置”；`active/detail` 响应也会带出当前 job message。首页 organize modal 和任务详情页现在都能识别 `error` phase，前端不会再在 error plan 上卡住轮询。`tests/integration/ai-organize-routes.test.ts` 已覆盖 missing-plan retry 优先级、error plan retry 和 error detail message。 | 后续若继续改 organize retry 路由、前端 `organizePhase` 状态机或任务详情页状态映射，必须先复跑 `npx vitest run tests/integration/ai-organize-routes.test.ts --reporter=verbose`、全量 `npm test` 与 `npm run build`，避免重新回到“error 已产生但前后端都不认识”的灰区。 |
| `RISK-028` | 2026-04-01 | `R7-AI-06` | AI organize error cancel mismatch | resolved | done | `R7-AI-06` | `c72c149` | 2026-04-01 20:53:07 +0800 | 2026-04-01 20:55:17 +0800 |  | `R7-AI-05` 之后首页 modal 已能展示 `error` phase，并继续提供“放弃”按钮；但状态机仍不允许 `error -> canceled`，前端 `cancelOrganize()` 也没有检查 HTTP 状态，导致 error plan 实际取消失败时仍可能被误报为“已取消”并直接关闭 modal。 | `R7-AI-06` 已允许 `error -> canceled` 的显式状态迁移，并修正首页 `cancelOrganize()` / `cancelAndRestart()` 对非 `2xx` 返回的处理。`tests/integration/ai-organize-routes.test.ts` 现在已覆盖“error plan 可取消且保留原 failed job 留痕”的合同。 | 后续若继续改 organize modal 动作按钮、`cancel` 路由或状态机迁移规则，必须先复跑 `npx vitest run tests/integration/ai-organize-routes.test.ts --reporter=verbose`、全量 `npm test` 与 `npm run build`，避免重新回到“按钮存在但实际不能取消”的假交互。 |
| `RISK-029` | 2026-04-01 | `R7-QA-07` | Historical browser regression drift | resolved | done | `R7-QA-07` | `402a323` | 2026-04-01 21:11:08 +0800 | 2026-04-01 22:10:31 +0800 |  | 之前各轮 issue 虽然已经各自验收，但浏览器证据分散在 MCP 旅程、独立 harness 和扩展 runtime 记录里，缺少一条能 clean rerun 的统一历史回放入口；同时扩展 popup 文案迭代后，旧 round-trip 脚本里的 `请配置 Token` / `快照已保存` 断言也开始漂移。 | `R7-QA-07` 已新增 `scripts/playwright-release-journeys-validate.ts`、`scripts/ai-organize-ui-validate.ts` 和统一入口 `scripts/playwright-issue-regression-validate.ts`，把 `R1/R2/R3/R4/R5/R6/R7` 里有浏览器表面的主要历史 issue 串成一条 clean rerun 浏览器矩阵；同时把 `scripts/extension-roundtrip-validate.ts` 的旧断言对齐到了当前 popup 文案 `待配置` / `已完成收藏和存档`。本轮 `npx playwright install chromium`、`npx tsx scripts/playwright-issue-regression-validate.ts`、`npx tsc --noEmit`、`npm test`、`npm run build` 全部通过。 | 后续若继续改首页交互、模板弹窗、设置页 AI 诊断、AI organize modal / 任务页，或扩展 popup / runtime 文案与行为，必须先复跑 `npx tsx scripts/playwright-issue-regression-validate.ts`、全量 `npm test` 与 `npm run build`；这条矩阵是历史 browser contract 的补充回放入口，但不意味着恢复 `e2e/` 为主 gate。 |
| `RISK-030` | 2026-04-01 | `R8-QA-01` | Backup restore and job detail browser drift | resolved | done | `R8-QA-01` | `45ce1ab` | 2026-04-01 22:43:18 +0800 | 2026-04-01 23:05:08 +0800 |  | 备份创建 / 命名还原和任务详情实时刷新虽然已有路由合同或散落的 MCP 旅程记录，但仍缺少可独立 clean rerun 的统一浏览器回放入口；一旦首页备份弹窗或任务详情脚本改动，容易重新出现“接口还对，但真实页面状态不同步或操作不可达”的漂移。 | `R8-QA-01` 已补齐首页备份弹窗与任务详情页稳定选择器，新增 `scripts/backup-job-browser-validate.ts`，并把它接回 `scripts/playwright-issue-regression-validate.ts`。本轮已证明“立即备份 -> 命名还原 -> 页面刷新恢复书签数据”和“任务详情运行中刷新 -> 终态完成”都能在真实浏览器里 clean rerun；`tests/integration/page-assets.test.ts`、`npx tsc --noEmit`、`npm test`、`npm run build` 也已同步通过。 | 后续若继续改首页备份弹窗、partial-restore 页面提示、任务详情页脚本或 jobs SSE / polling 映射，必须先复跑 `npx tsx scripts/backup-job-browser-validate.ts`、`npx tsx scripts/playwright-issue-regression-validate.ts`、全量 `npm test` 与 `npm run build`。 |
| `RISK-031` | 2026-04-01 | `R8-QA-02` | Jobs and snapshots destructive-action browser drift | resolved | done | `R8-QA-02` | `17b5eea` | 2026-04-01 23:16:06 +0800 | 2026-04-02 12:11:58 +0800 |  | 任务列表的 `清理已完成 / 清空全部` 和快照页的 `批量删除` 之前仍主要靠路由合同和页面脚本维持；一旦前端确认弹窗、按钮禁用态、列表刷新或统计数字漂移，容易出现“后端已经删了，但页面没同步”或“页面显示成功，但真实数据没删干净”的错位。 | `R8-QA-02` 已为 `jobs` / `snapshots` destructive action 补齐稳定选择器，新增 `scripts/jobs-snapshots-browser-validate.ts`，并把它接回 `scripts/playwright-issue-regression-validate.ts`。本轮已在真实浏览器里证明 `clear-completed`、`clear-all`、`select-all + batch-delete` 的确认弹窗、页面刷新、数据库结果与快照文件清理全部一致；`tests/integration/page-assets.test.ts`、`npx tsc --noEmit`、`npm test` 与 `npm run build` 也已同步通过。 | 后续若继续改任务列表脚本、快照页筛选 / 删除脚本、确认弹窗接法或列表刷新逻辑，必须先复跑 `npx tsx scripts/jobs-snapshots-browser-validate.ts`、`npx tsx scripts/playwright-issue-regression-validate.ts`、全量 `npm test` 与 `npm run build`。 |
| `RISK-032` | 2026-04-02 | `R8-QA-03` | Import and export browser drift | resolved | done | `R8-QA-03` | `92efb1c` | 2026-04-02 13:36:21 +0800 | 2026-04-02 14:34:19 +0800 |  | 首页导入和导出虽然已有路由合同与零散 UI 功能，但导入文件上传、进度弹层收口、导出范围 / 格式切换和真实下载文件内容此前仍缺独立浏览器回放；一旦表单字段、前端状态切换或下载参数拼接漂移，容易出现“按钮能点但数据不对”的假成功。 | `R8-QA-03` 已为首页导入表单、导入进度弹层和导出弹层补齐稳定选择器，新增 `scripts/import-export-browser-validate.ts`，并把它接回 `scripts/playwright-issue-regression-validate.ts`。本轮已在真实浏览器里证明上传导入、进度收口、首页刷新、`all/html` 与 `uncategorized/json` 下载结果都正确；同时还用 Playwright MCP 在临时 smoke 环境里复验了登录后导入控件、覆盖分类下拉和导出弹层默认下载合同。`tests/integration/page-assets.test.ts`、`npx tsc --noEmit`、`npm test` 与 `npm run build` 也已同步通过。 | 后续若继续改首页导入表单、导入进度弹层、导出弹层或 `/export` 参数拼接逻辑，必须先复跑 `npx tsx scripts/import-export-browser-validate.ts`、`npx tsx scripts/playwright-issue-regression-validate.ts`、全量 `npm test` 与 `npm run build`；若改的是首页交互可见性，还应再跑一轮 Playwright MCP 页面复验。 |
| `RISK-033` | 2026-04-02 | `R9-QA-01` | Backup upload restore browser drift | resolved | done | `R9-QA-01` |  | 2026-04-02 19:37:47 +0800 | 2026-04-02 20:28:41 +0800 |  | 首页备份弹窗虽然已有“立即备份 -> 命名还原”的浏览器回放，但“上传 `.db` 还原”和“删除备份”仍主要靠 API 合同与页面壳体选择器维持；一旦 multipart 提交、确认弹窗或列表刷新漂移，容易出现“接口可用但真实页面无法恢复 / 误删未刷新”的错位。 | `R9-QA-01` 已补一条独立浏览器 harness `scripts/backup-upload-delete-browser-validate.ts`，并把它接回 `scripts/playwright-issue-regression-validate.ts`；同时把 `backup-upload-input`、`backup-upload-submit` 壳体断言补进 `tests/integration/page-assets.test.ts`。本轮已在真实浏览器里证明上传 `.db` 还原、成功提示、页面刷新恢复书签 / 分类，以及删除手动备份后的列表刷新与磁盘文件清理全部成立；同轮还用 Playwright MCP 在临时 smoke 环境里复验了“创建手动备份 -> 扰动临时数据 -> 上传同一 `.db` 还原 -> 删除该手动备份”的页面合同。 | 后续若继续改首页备份弹窗、上传 restore、多段确认提示或备份列表刷新逻辑，必须先复跑 `npx tsx scripts/backup-upload-delete-browser-validate.ts`、`npx tsx scripts/playwright-issue-regression-validate.ts`、全量 `npm test` 与 `npm run build`；若改的是首页备份交互可达性，还应再跑一轮 Playwright MCP 页面复验。 |
| `RISK-034` | 2026-04-02 | `R9-QA-02` | Snapshot browse and download browser drift | resolved | done | `R9-QA-02` |  | 2026-04-02 20:33:33 +0800 | 2026-04-02 20:52:49 +0800 |  | 快照页当前只有批量删除拿到了真实浏览器证据；搜索 / 日期筛选、单条查看、真实下载和单条删除仍主要靠路由合同与页面壳体维持，一旦链接参数、列表筛选或文件名编码漂移，容易出现“按钮还在，但打开 / 下载 / 删除结果不对”的假成功。 | `R9-QA-02` 已为快照页补齐 `snapshot-search-input`、`snapshot-date-filter`、`snapshot-clear-filter`、`snapshot-view-link`、`snapshot-download-link` 等稳定选择器断言，新增独立浏览器 harness `scripts/snapshot-browse-download-browser-validate.ts`，并把它接回 `scripts/playwright-issue-regression-validate.ts`。本轮已在真实浏览器里证明搜索 + 日期筛选收敛、快照查看页命中目标文件、真实下载文件名 / 内容正确，以及单条删除后的列表刷新、数据库记录与文件系统结果一致；同轮还用 Playwright MCP 在临时 smoke 环境里复验了快照页筛选、查看内容、下载链接合同与单条删除后的列表 / 文件变化。 | 后续若继续改快照页筛选脚本、查看 / 下载链接拼接、删除模态框或列表刷新逻辑，必须先复跑 `npx tsx scripts/snapshot-browse-download-browser-validate.ts`、`npx tsx scripts/playwright-issue-regression-validate.ts`、全量 `npm test` 与 `npm run build`；若改的是快照页交互可达性，还应再跑一轮 Playwright MCP 页面复验。 |
| `RISK-035` | 2026-04-02 | `R9-QA-03` | Generic job cancel and failure UI drift | open | todo | `R9-QA-03` |  |  |  |  | 首页导入进度弹层取消、顶部当前任务取消、任务详情取消和失败明细分页当前仍主要靠 API / SSE 合同与页面壳体维持；一旦前端按钮禁用态、轮询收口或分页刷新漂移，容易出现“后端已取消但前端仍显示运行中”或“失败列表 API 正确但页面翻页不对”的错位。 | `tests/integration/jobs-routes.test.ts` 已覆盖 `/api/jobs/:id/cancel`、`/jobs/:id/failures` 和 SSE，`tests/integration/import-routes.test.ts` 已覆盖导入主合同，`tests/integration/page-assets.test.ts` 已固定 `cancel-job-btn`、`import-progress-cancel` 壳体，`R8-QA-01` / `R8-QA-03` 已证明任务详情刷新与导入成功主线成立。 | 承接 `R9-QA-03`，新增独立浏览器 harness 覆盖导入取消、通用任务取消与失败明细分页，并接回 `scripts/playwright-issue-regression-validate.ts`；完成后复跑 `npx tsc --noEmit`、`npm test`、`npm run build`。 |
