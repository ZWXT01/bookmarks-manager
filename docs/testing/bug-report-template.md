# 问题反馈模板（Bug Report）

> 建议复制本模板填写；请尽量提供**可复现步骤**与**最小复现数据**。涉及数据破坏/安全问题请按“安全问题”段落补充敏感信息处理方式。

---

## 1. 基本信息
- 标题（必填）：`[模块] 一句话描述问题`
- 严重级别（必填）：S0（阻断/数据丢失/安全）| S1（主要功能不可用）| S2（功能异常但可绕过）| S3（体验/文案）
- 优先级（必填）：P0 | P1 | P2 | P3
- 问题类型：功能 | 兼容性 | 性能 | 安全 | 数据一致性 | UI/交互 | 其他
- 影响范围：书签 | 分类 | 导入导出 | 检查 | 任务/SSE | 备份/还原 | 快照 | AI | 设置 | 扩展 | 登录/Token

---

## 2. 环境信息（必填）
- 版本/提交（若有）：`git rev-parse HEAD` 输出
- 部署方式：Docker | 本地 `npm run dev` | 本地 `npm start`
- 操作系统：Windows | macOS | Linux（发行版/版本）
- 浏览器：Chrome/Edge/Firefox 版本号（扩展问题必填）
- Node.js 版本（本地部署必填）：`node -v`
- 关键环境变量（如有改动）：
  - `DB_PATH=`
  - `PORT=`
  - `API_TOKEN=`（只填 prefix，不要贴明文）
  - `SESSION_SECRET=`（不要贴明文）
  - 备份/检查/定期检查相关配置

---

## 3. 复现步骤（必填）
1. 
2. 
3. 

### 复现数据（可选但强烈建议）
- 书签 URL/标题（示例）：`https://example.com`
- 分类路径/ID：
- 导入文件（建议附上最小化样本：HTML/JSON/TXT）：
- 任务 ID（JobId）：
- 备份文件名 / 快照文件名：

---

## 4. 期望结果 vs 实际结果（必填）
**期望结果**
- 

**实际结果**
- 

**复现概率**
- 必现 | 高（>70%）| 中（30~70%）| 低（<30%）| 偶现

---

## 5. 影响评估（建议填写）
- 是否数据丢失/覆盖：是/否（若是，说明范围）
- 是否安全风险：是/否（若是，见“安全问题”段落）
- 是否可绕过：是/否（绕过方案）
- 回归信息：之前版本是否正常？从哪个版本开始出现？

---

## 6. 证据与日志（强烈建议）

### UI 证据
- 截图/录屏：
- 浏览器 Console 报错：
- Network 面板关键请求（可导出 HAR，注意脱敏）：

### 服务端日志
- 相关时间段日志（建议包含 requestId/jobId）：

### API 请求/响应（可选，建议对接口类问题填写）
- 请求：
  - Method/URL：
  - Headers（脱敏）：
  - Body（脱敏）：
- 响应：
  - Status：
  - Body：

---

## 7. 初步定位/建议（可选）
- 怀疑的模块/文件：
- 可能的触发条件（时序/并发/数据状态）：
- 建议的修复方向：

---

## 8. 安全问题（仅当适用）
> 安全问题请不要在公开渠道粘贴可利用细节（例如明文 token、可利用 payload）。

- 漏洞类型：鉴权绕过 | 信息泄露 | 注入 | XSS | CSRF | 路径穿越 | SSRF | 其他
- 影响面：哪些接口/页面/数据？
- 可利用性：需要登录吗？需要 Token 吗？是否可远程利用？
- 复现步骤（脱敏版）：
- 建议修复：最小权限、输入校验、输出编码、文件名白名单、重定向校验等

